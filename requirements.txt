# Requirements for LND Monitor - Both Versions
# Core dependencies for original version
requests>=2.31.0
requests[socks]>=2.31.0
urllib3>=2.0.0

# New dependencies for PTB version
python-telegram-bot>=20.7
aiohttp>=3.9.0
aiohttp-socks>=0.8.0

# Optional: For better async performance
uvloop>=0.19.0; sys_platform != "win32"
