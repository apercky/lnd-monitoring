# =============================================================================
# LND Monitoring Configuration Example
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# =============================================================================
# TELEGRAM CONFIGURATION (Required)
# =============================================================================
# Get your bot token from @BotFather on Telegram
# Example: Create a new bot with /newbot command
TELEGRAM_BOT_TOKEN=<your_bot_token>

# Your Telegram Chat ID (get from @userinfobot)
# Can be your personal chat ID or a group chat ID
TELEGRAM_CHAT_ID=<your_chat_id>

# =============================================================================
# START9 LND NODE CONFIGURATION (Required)  
# =============================================================================
# Your Start9 LND node .onion address (without http:// or port)
# Example: Get this from your Start9 dashboard -> LND service
LND_NODE_ONION_ADDRESS=<your_address.onion>

# Base64 encoded readonly macaroon from your Start9 LND node
# Example: Export readonly macaroon from Start9 dashboard, then: base64 -w 0 readonly.macaroon
LND_MACAROON_RO=<your_macaroon_readyonly_base64>

# =============================================================================
# OPTIONAL CONFIGURATION (Has defaults)
# =============================================================================
# LND REST API port (default: 8080)
LND_NODE_PORT=8080

# Check interval in seconds (default: 120 = 2 minutes)
CHECK_INTERVAL=120

# HTTP request timeout in seconds (default: 30)
TIMEOUT=60

# Connection timeout
CONNECTION_TIMEOUT=45

# Refresh Tor circuits every 5 minutes
CIRCUIT_REFRESH_INTERVAL=300

# Maximum retry attempts before considering node offline (default: 3)
MAX_RETRIES=3

# URL to test Tor connectivity (default: http://check.torproject.org/api/ip)
TOR_CHECK_URL=http://check.torproject.org/api/ip

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. TELEGRAM BOT:
#    - Message @BotFather on Telegram
#    - Create new bot: /newbot
#    - Follow instructions and get your bot token
#    - Message @userinfobot to get your chat ID
#
# 2. START9 LND NODE:
#    - Access your Start9 node dashboard
#    - Go to LND service settings  
#    - Copy the .onion address (without http:// or port)
#    - Export readonly macaroon from Start9 dashboard
#    - Convert to base64: base64 -w 0 readonly.macaroon
#
# 3. COPY AND CONFIGURE:
#    - Copy this file: cp env.example .env
#    - Edit .env with your real values: nano .env
#    - Never commit .env to git (already in .gitignore)
#
# 4. DEPLOY:
#    - docker-compose up -d
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Use READONLY macaroon only (never admin macaroon)
# - Keep .env file secure and never share it
# - .env is excluded from git by .gitignore
# - All communication goes through Tor for privacy
# - Monitor logs for any security issues: docker logs lnd-monitor
